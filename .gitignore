.DS_Store
.gstack/

# Build output
dist/
.astro/

# Dependencies
node_modules/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment / secrets
*.env
!*.env.example

# Private knowledge base (local read-only symlink to mixt-brain).
# NEVER commit: this repo is public, the brain is private medical/business data.
# Each dev creates the symlink locally: ln -s /path/to/mixt-brain .brain
.brain
.brain/

# Local agent instructions — references the private brain, keep out of the public repo.
CLAUDE.md

# IDE
.idea/
.vscode/

# AI image generation — archive locale, pas pushée
public/images/generated/*
!public/images/generated/.gitkeep
