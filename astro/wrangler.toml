name = "mixt-landing"
compatibility_date = "2024-10-01"

# Static site hosting — serves files from dist/ directly
# not_found_handling : cherche un 404.html dans dist/ (on en crée un via Astro)
[assets]
directory = "./dist"
not_found_handling = "404-page"
